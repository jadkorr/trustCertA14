name: Releases

on: 
  push:
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Zip Folder
      run: zip -r trustCertA14.zip .
    - name: Release to Github
      run: echo "Release"
